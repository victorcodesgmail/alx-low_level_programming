#include <stdio.h>
/**
 * main-main
 * @argc: no of arguments
 * @argv: name of command and commandline arguments
 * Return: int
 */
int main(int argc, char *argv[])
{
printf("%s\n", argv[argc - 1]);
return (0);
}
